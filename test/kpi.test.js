/* global describe, it */
var assert = require('chai').assert;
var kpi = require('../js/kpi.js').kpi;

describe('KPI Doughnut', function() {
    
    describe('getDoughnutData method', function() {
        it('returns 75 when actual is less than 80%', function() {
            var test = "test";
            assert.equal(test, "test");
        });
        /*
        it('returns a.b when input is a.a', function() {
            var test = "test";
            assert.equal(test, "test");
        });
        // */
    });
    /*
    describe('getParentPath method', function() {
        it('returns empty when input is undefined', function() {
            
        });
    });
    // */
    
});